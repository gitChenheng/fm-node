// select
let user=await User.findAll();
console.log(JSON.stringify(user))
(async () => {
    let users = await User.findAll({where: {name: 'chen'}});
    console.log(`find ${pets.length} pets:`);
    for (let p of users) {
        console.log(JSON.stringify(p));
    }
})();

// update
(async ()=>{
    let p=await User.findOne({where:{name:'chen'}});
    p.gender=true;
    await p.save();
})();

//insert
(async ()=>{
    await User.create(body);
})();

// delete
(async ()=>{
    let p = User.findOne({where:{name:'chen'}});
    await p.destroy();
})()
